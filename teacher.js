const out=document.querySelector('#lessons');function fmt(d){return new Date(d).toLocaleString('ru-RU',{day:'2-digit',month:'short',hour:'2-digit',minute:'2-digit'});}async function load(){const r=await fetch('/api/teacher/dashboard');if(r.status===403){location.href='/';return;}const d=await r.json();document.querySelector('#teacherName').textContent=d.profile.name;document.querySelector('#teacherMeta').textContent=[d.profile.position,d.profile.department].filter(Boolean).join(' · ');document.querySelector('#teacherEmail').textContent=d.profile.email;out.innerHTML=d.lessons.length?d.lessons.map(l=>`<article class="lesson"><div><strong>${l.courseCode} · ${l.courseName}</strong><div>${l.title}</div><div class="meta">Группа ${l.groupName} · ${fmt(l.startsAt)} · ${l.status}</div></div><div class="actions"><button class="primary" onclick="location.href='/video.html'">Начать онлайн-занятие</button></div></article>`).join(''):'<p>Занятий пока нет.</p>';}document.querySelector('#logout').onclick=async()=>{await fetch('/api/logout',{method:'POST'});location.href='/';};load().catch(()=>location.href='/');