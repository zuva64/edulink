const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const out=document.querySelector('#lessons');function fmt(d){return new Date(d).toLocaleString('ru-RU',{day:'2-digit',month:'short',hour:'2-digit',minute:'2-digit'});}async function load(){const r=await fetch('/api/teacher/dashboard');if(r.status===403){location.href='/';return;}const d=await r.json();document.querySelector('#teacherName').textContent=d.profile.name;document.querySelector('#teacherMeta').textContent=[d.profile.position,d.profile.department].filter(Boolean).join(' · ');document.querySelector('#teacherEmail').textContent=d.profile.email;out.innerHTML=d.lessons.length?d.lessons.map(l=>`<article class="lesson"><div><strong>${esc(l.courseCode)} · ${esc(l.courseName)}</strong><div>${esc(l.title)}</div><div class="meta">Группа ${esc(l.groupName)} · ${fmt(l.startsAt)} — ${fmt(l.endsAt)} · ${esc(l.status)}</div></div><div class="actions"><a class="ghost" href="/journal.html?lesson=${l.id}">Журнал занятия</a><a class="ghost" href="/learning.html?lesson=${l.id}">План и тестирование</a><button class="primary" ${l.status !== 'Запланировано' ? 'disabled' : ''} onclick="location.href='/video.html'">Начать онлайн-занятие</button></div></article>`).join(''):'<p>Занятий пока нет.</p>';}document.querySelector('#logout').onclick=async()=>{await fetch('/api/logout',{method:'POST'});location.href='/';};load().catch(()=>location.href='/');